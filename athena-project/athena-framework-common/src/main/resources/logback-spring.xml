<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- 日志级别变量（可选） -->
    <!-- 定义日志级别 -->
    <property name="LOG_LEVEL" value="INFO"/>
    <!-- 定义日志目录 -->
    <property name="LOG_PATH" value="logs"/>
    <!-- 定义日志文件名 -->
    <property name="LOG_FILE_NAME" value="app.log"/>
    <!-- 日志文件大小 -->
    <property name="MAX_FILE_SIZE" value="100MB"/>
    <!-- 日志文件数量 -->
    <property name="MAX_BACKUP_FILES" value="10"/>

    <!-- 彩色日志依赖 -->
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex" converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="highlight" converterClass="org.springframework.boot.logging.logback.HighlightingCompositeConverter"/>

    <!-- 自定义日志格式 -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId}] %-40.40logger{39}:%L %highlight(%-5level) - %msg%n"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE_NAME}</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archived.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_BACKUP_FILES}</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Root Logger -->
    <root level="${LOG_LEVEL}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
