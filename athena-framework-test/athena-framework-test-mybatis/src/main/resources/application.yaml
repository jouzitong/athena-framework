server:
  port: 20010

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/app_user?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      max-active: 5
      initial-size: 3


lib:
  mapper:
    auto-gen:
      enable-create-table-ddl: true
      type: mysql